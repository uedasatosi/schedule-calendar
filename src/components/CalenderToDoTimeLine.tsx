import { Task } from "@mui/icons-material";
import { Box, Stack } from "@mui/system";

export default function ToDoTimeLine () {
    const Task = [
        {
            todo:"work"
        }
    ]
    return <Stack>
    {
        Task.map((todo,index)=>(<Box>
            demodate
        </Box>))
    }
</Stack>
}
  